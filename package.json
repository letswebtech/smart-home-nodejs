{
    "name": "smart-home-nodejs",
    "version": "1.0.0",
    "description": "Smart Home WebSocket Server",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "mkdir -p /opt/render/project/src && cp -r ./* /opt/render/project/src/"
    },
    "dependencies": {
        "express": "^4.18.2",
        "socket.io": "^2.5.0",
        "cors": "^2.8.5"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    },
    "engines": {
        "node": ">=14.0.0"
    }
}